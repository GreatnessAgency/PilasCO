image: mwienk/docker-lftp

build:
  script:
    - pwd
    - ls
    - lftp -e "set ssl:verify-certificate no; open $FTP_SERVER; user '$FTP_USERNAME','$FTP_PASSWORD'; mirror -X .* -X .*/ --reverse --verbose --delete ./ /home/pilascol/test_ci/; bye"